# Cryptography-Algorithms
It contains all the cryptographic algorithm implementation in python . Encrypted data and key for decryption is gets stored in .pickle file  so no other person can read that and privacy is maintained.
